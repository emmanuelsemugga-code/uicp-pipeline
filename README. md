# UICP – External Verification Package (Phases 1‑5)

## What this package contains

This package provides public interfaces and automated verification scripts
for all five phases of the UICP constraint integrity pipeline.It allows
independent engineers and AI labs to confirm every claim made by the system
without accessing any internal algorithms, pipeline logic, or design maps.

## Files included

Phase 1 – Structural Normalization
  - normalize.py                   (public wrapper)
  - verify_phase1_claims.py        (26‑check adversarial verification)
  - Phase 1 External Validation Document (human‑readable)

Phase 2 – Single‑Variable Semantic Analysis
  - phase2_public.py               (public wrapper)
  - verify_phase2_claims.py        (14‑check adversarial verification)
  - Phase 2 External Validation Document (human‑readable)

Phase 3 – Multi‑Variable Canonicalization
  - phase3_public.py               (public wrapper)
  - verify_phase3_claims.py        (21‑check adversarial verification)
  - Phase 3 External Validation Document (human‑readable)

Phase 4 – Enforcement Gateway
  - phase4_public.py               (public wrapper)
  - verify_phase4_claims.py        (43‑check adversarial verification)
  - Phase 4 External Validation Document (human‑readable)

Phase 5 – Trust and Audit Layer
  - phase5_public.py               (public wrapper)
  - verify_phase5_claims.py        (30‑check adversarial verification)
  - Phase 5 External Validation Document (human‑readable)

Integration Layer
  - binding_extraction.py          (17‑check validated extraction layer)
  - decision_export.py             (audit bundle export module)
  - demo_live_enforcement.py       (live‑enforcement demonstration script)

Master scripts
  - verify_all_phases.py           (single‑command pipeline verification)
  - HOW_TO_VERIFY.txt              (plain‑text quick‑start instructions)

## How to run

To validate the entire pipeline with one command:
  python3 verify_all_phases.py

1. Place all .py files in a single directory alongside the internal engines
   (normalize_v05.py, phase2_engine.py, phase3_engine.py,
    phase4_engine.py, phase5_engine.py).
   The internal engines are provided under controlled disclosure, not in this
   public package.

2. Run the master verification script:
     python3 verify_all_phases.py

3. To verify a single phase:
     python3 verify_phase1_claims.py
     python3 verify_phase2_claims.py
     python3 verify_phase3_claims.py
     python3 verify_phase4_claims.py
     python3 verify_phase5_claims.py

## What each phase proves

Phase 1: syntactic normalization — determinism, boundedness, termination,
         fixed‑point, commutativity, absorption, identity bijection,
         serialization invariance, structural stability, escape containment.

Phase 2: single‑variable semantics — equivalence, dominance, conflict,
         execution, OUT_OF_SCOPE preservation, multi‑variable extended
         fields, identity‑ledger completeness.

Phase 3: multi‑variable canonicalization — redundancy elimination, conflict
         detection, nonlinear preservation, determinism, identity‑ledger
         completeness.

Phase 4: runtime enforcement — contract integrity, binding validation,
         enforceable constraint checking, non‑enforceable constraint
         pass‑through, identity immutability, determinism, cryptographic
         chain integrity, sanitised model‑facing response, output contract
         compliance.

Phase 5: trust and audit — objective commitment with Ed25519 signatures,
         verifiable compliance proofs, human‑gated override controls,
         append‑only audit log with cryptographic chaining, private‑key
         safety enforcement, third‑party proof verification without
         engine access.

## Live Demonstration

A hard‑verdict live demo using Groq / Llama 3.1 is recorded and available at:
  https://youtube.com/shorts/5bcrtv_1pMQ

## Disclosure boundary

This public package contains ONLY the public wrappers, verification scripts,
and human‑readable validation documents.  The internal engines, all
specification documents, adversarial exchange artifacts, design maps,
and pipeline algorithms are retained internally and are NOT part of this
package.  This boundary is intentional and non‑negotiable.

